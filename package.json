{
	"name": "padnos-careers",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"build": "wp-scripts build",
		"start": "wp-scripts start",
		"dev": "npm-run-all --parallel sync start",
		"sync": "browser-sync start -p 'plugin.local' --files '**/*.php' 'build/*.js' 'build/*.css'",
		"devFast": "wp-scripts start",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@wordpress/scripts": "*",
		"axios": "^0.21.1",
		"normalize.css": "^8.0.1"
	},
	"devDependencies": {
		"webpack": "^5.51.2",
		"webpack-cli": "^4.8.0",
		"browser-sync": "^2.27.4",
		"npm-run-all": "^4.1.5"
	}
}
